/* responsive header*/
@media screen and (max-width: 960px) {
  /* header */
  .header ul {
    display: none;
  }
  .header .menu-mobile-user {
    display: inline-block;
  }
  .header {
    justify-content: center;
    align-items: center;
  }
}
/* responsive svg and h1 in homepage */
@media screen and (max-width: 750px) {
  /* svg(ferrari logo) */
  .header svg {
    width: 30px;
  }
  /* h1 homepage */
  .homePage .container-box h1 {
    font-size: 25px;
  }
  /* responsive video player section */
  .video-section .slide-video .box-on-video h1 {
    font-size: 17px;
  }
  /* responsive slide photos section */
  .slider-photo-container .buttonSlider{
    display: none;
  }
  .slider-photo-container{
    height: auto;
    min-height: 100vh;
    overflow: visible;
  }
  .slide-photo {
    display: flex !important;  
    flex-direction: column-reverse;
    align-items: center;
    gap: 10px;
    height: auto;
  }
  .slide-photo .left-slider{
    padding: 0 20px;
    gap: 15px;
  }
  .slide-photo .left-slider .title{
    font-size: 20px;
  }
  .slide-photo .right-slider{
    height: 300px;
  }
  .slide-photo .left-slider,
  .slide-photo .right-slider {
    width: 100%;
    height: 50%;
    border-radius: 0;
    box-shadow: none;
  }
}